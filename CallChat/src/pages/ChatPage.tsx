import { useAppSelector } from '../app/hooks'
export function ChatPage() { const user = useAppSelector((state) => state.auth.user)!; return <section className="welcome-card"><div className="avatar">{user.displayName.charAt(0).toUpperCase()}<span className="online-dot" /></div><p className="eyebrow">Đăng nhập thành công</p><h1>Xin chào, {user.displayName}!</h1><p className="welcome-copy">Tài khoản @{user.username} đã sẵn sàng. Khu vực trò chuyện sẽ được phát triển trong feature conversations và messages.</p><div className="coming-soon"><span>💬</span><div><strong>Không gian trò chuyện</strong><p>Tính năng chat đang được phát triển</p></div></div></section> }
